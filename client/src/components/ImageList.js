import React from 'react'
import env from "../config/env"

const ImageList = () => {
    console.log(env.ENDPOINT)
    return (
        <div>
            images       
        </div>
    )
}

export default ImageList
